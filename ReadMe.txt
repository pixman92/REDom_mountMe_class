REDOM made into a CLass operation

- var foo = new MountMe();
- foo.make(<query>, '<inner HTML>', <HTML to Mount to>);

- The operations is as follows:
  - make a new Obj
  - add a query of that, one such 'new' mounts on to another HTML query
  
- each time adding DIVs
- each DIV being added to a specific HTML Query




=======
Code to change up Attributes of the REDom code
- document.querySelector('').setAttribute('', '');
